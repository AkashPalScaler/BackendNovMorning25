package Streams;

import java.util.function.Function;

public class SomeFunClass implements Function<Integer, Double> {
    @Override
    public Double apply(Integer integer) {
        return 0.0;
    }
}
